<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <CheckBoxPreference
        android:disableDependentsState="true"
        android:key="@string/KEY_12H"
        android:summaryOff="@string/off_12h"
        android:summaryOn="@string/on_12h"
        android:title="@string/title_12h" />
    <!-- Simply we want to use the standard fonts only, so comment this preference option
    <CheckBoxPreference
        android:disableDependentsState="true"
        android:key="@string/KEY_STANDARD_FONTS"
        android:summaryOff="@string/off_standard_fonts"
        android:summaryOn="@string/on_standard_fonts"
        android:title="@string/title_standard_fonts" />
    -->

</PreferenceScreen>